# 개발 가이드라인

이 문서는 MMD Anju의 공통 작업 기준입니다. 특정 AI 도구에 종속되지 않습니다.
프로젝트 구조는 [MAP.md](MAP.md), 실행·릴리스 명령은 [README.md](README.md)를 참고합니다.

## UI와 아이콘

- UI 아이콘은 **Google Material Symbols Rounded**, **굵기 300**을 사용합니다.
- 아이콘 스타일에 `font-family: 'Material Symbols Rounded'`와 `font-variation-settings: 'wght' 300`을 적용합니다.
- 새 아이콘은 이 계열에서 선택하고, 별도 아이콘 라이브러리나 직접 그린 SVG를 혼용하지 않습니다.
- 기존 플레이어의 회색 패널, 얇은 테두리, 작은 모서리, 버튼 스타일을 따릅니다. `index.html`의 공통 CSS 변수를 우선 사용합니다.
- 아이콘만 있는 버튼에는 동작을 설명하는 접근성 이름을 제공합니다. 키보드 포커스와 모바일 레이아웃을 확인합니다.
- 재생 불가 안내 카드에는 ‘다시 시도’ 버튼을 넣지 않습니다.

## 구현과 확인

- 기존 Vanilla JavaScript ES modules 구조를 따릅니다. 기능 변경에 필요하지 않은 의존성이나 빌드 도구는 추가하지 않습니다.
- 브라우저 이름만으로 재생을 차단하지 않고 WebGPU 지원과 초기화 결과를 기준으로 판단합니다.
- 변경 범위에 맞게 확인합니다. 문서만 수정했다면 링크와 diff를 확인하고, 동작 변경은 관련 테스트를 실행합니다. UI 변경은 데스크톱과 모바일 화면을 확인합니다.
- 런타임 변경 또는 배포 전에는 다음 검증을 실행합니다.

```bash
node --test tests/*.test.mjs
python3 scripts/build_site.py
git diff --check
```

- 빌드 스크립트는 Git에 등록된 런타임 파일만 포함합니다. 새 파일은 추적 상태를 확인하고, 로컬 샘플이나 아카이브가 섞이지 않도록 합니다.
- 실제 GPU 재생을 확인하지 않았다면 테스트 통과와 실제 재생 검증을 구분해 보고합니다.

## 커밋과 배포

- **사용자가 명시적으로 배포를 요청했을 때만 배포합니다.** 일반적인 수정 요청은 로컬 수정과 검증까지만 진행합니다.
- 커밋·푸시도 별도 요청 범위에 맞춰 진행합니다. 수정했다고 자동으로 커밋하거나 푸시하지 않습니다.
- `main` 푸시는 검증만 수행합니다. `vMAJOR.MINOR.PATCH` 태그 푸시는 실제 배포와 릴리스 노트 발행을 유발하므로 배포 요청 없이 실행하지 않습니다.
- 배포 대상은 `_site/`입니다. 저장소 루트나 로컬 `data` 아카이브를 배포하지 않습니다.
- `deployed: false`는 샘플의 UI 표시 설정이며 파일 접근 제한이 아닙니다. 로컬 전용 샘플은 Git에 추가하지 않습니다.
- 수동 릴리스 노트가 필요하면 태그 생성 전에 `releases/<tag>.md`를 작성합니다. 상세 절차는 README를 따릅니다.

## 문서 유지

- 개발 규칙은 이 문서에, 코드 위치와 문서 연결은 MAP.md에 기록합니다.
- AGENTS.md와 CLAUDE.md에는 진입 링크만 두어 규칙이 서로 달라지지 않게 합니다.
